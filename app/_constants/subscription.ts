/** Duração do trial de premium para usuários novos. */
export const PREMIUM_PLAN_TRIAL_DAYS = 14;

/** Período de tolerância concedido a usuários pagantes legados (antes da migração premiumUntil). */
export const PREMIUM_PLAN_LEGACY_GRACE_DAYS = 30;

/** Cobertura concedida a cada pagamento recebido da assinatura mensal. */
export const PREMIUM_PLAN_PAYMENT_COVERAGE_DAYS = 30;

export const PREMIUM_PLAN_PRICE = 19.9;
